[tools]
go = "latest"
bun = "latest"
biome = "latest"
air = "latest"

[env]
# CGO_ENABLED is set explicitly in Makefile/Dockerfile for static builds
# to avoid interfering with `go test -race` which requires CGO
